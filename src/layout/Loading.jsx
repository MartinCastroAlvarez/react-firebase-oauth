/**
 * React component for displaying a loading message.
 *
 * This component displays a message or icon indicating that the application is loading or
 * processing data. The component may be used to inform the user that the application is
 * doing something in the background and that they should wait until the task is complete.
 *
 * The component may use different types of loading messages, such as spinners, progress bars,
 * or text messages, depending on the context and the duration of the loading process. The
 * component may also include additional functionality, such as cancel buttons or retry links,
 * to provide the user with more control over the loading process.
 */

const Loading = () => {
    return (
        <div className="View">
            <h1>Loading</h1>
        </div>
    )
}

export default Loading
